name: Validate story graph

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-story:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate story graph and smoke routes
        run: node scripts/validate-story.mjs
